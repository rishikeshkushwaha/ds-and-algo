class Solution(object):
    def sequentialDigits(self, low, high):
        """
        :type low: int
        :type high: int
        :rtype: List[int]
        """
        s = '123456789'
        ans=[]
        str_low = str(low)
        str_high = str(high)
        for i in range(len(str_low), len(str_high) + 1):
            for j in range(len(str_low),len())
        return ans
sol= Solution()
low=1000
high = 9999
print(sol.sequentialDigits(low, high))